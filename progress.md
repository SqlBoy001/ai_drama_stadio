# Progress

## 2026-09-16
- 已阅读用户提供的开发计划。
- 已确定本轮交付范围：完成可运行的本地 Mock MVP，而非仅做 Phase 0 审计。
- 已创建持久化实施计划、发现记录与进度日志。
- 已获取 LocalMiniDrama 1.2.8 源码并创建 `codex/ai-workbench` 分支。
- 已阅读仓库级 `AGENTS.md`、README、LICENSE、SECURITY 与近期 CHANGELOG。
- 已新增 Agent 工作台数据库迁移、计划器、运行状态机、三阶段审核、预算、使用量和质检记录接口。
- 已新增前端 `/agent-workbench` 页面、API 封装、项目首页入口，以及从自然语言到 Mock 生产闭环的交互。
- 当前进入验证阶段：依赖安装、迁移、测试、构建和页面级检查。
- 首次并行执行前后端 `npm ci` 因沙箱 DNS 无法访问锁文件中的镜像源而失败，准备在获批的联网环境重试。
- 前端依赖安装成功；后端在 Node 26 上因 `better-sqlite3` 无预编译包且本机缺少 Xcode CLT 而失败，正在切换到项目声明支持且有预编译产物的 Node LTS。
- 使用 Node 22 LTS 成功安装后端依赖并执行迁移。
- 最终后端测试 76/76 通过，前端测试 10/10 通过，Vite 生产构建成功，`git diff --check` 通过。
- HTTP 端到端验证完成：创建 3 集项目并依次通过剧本、资产和成片审核，最终状态 `EXPORTED`；24 条用量与 24 条质检记录落库。
- 使用浏览器验证完成态看板、新建创作页与计划确认弹窗，控制台无报错；截图保存为 `workbench-preview.png` 与 `workbench-plan-preview.png`。
- 已关闭浏览器和本地测试服务。

## 2026-09-16 启动链路修复
- 已复核用户报告的 ABI 错误：Node 22=127，Node 26=147；当前 `better-sqlite3` 可由显式 Node 22 正常加载。
- 已确认正常 NVM shell 中 Node/npm 一致；用户现场 npm 版本异常，因此采用显式运行时路径而非继续依赖 `npm start`。
- 已新增可执行的根目录 `start_app.command`：自动定位 Node 22/20、固定 lifecycle PATH、校验/重建 SQLite、按需安装依赖和构建前端，并直接用明确的 Node 可执行文件启动后端。
- 红色回归检查：`AI_DRAMA_NODE=/opt/homebrew/bin/node ./start_app.command --check` 能拒绝 Node 26。
- 绿色回归检查：`./start_app.command --check` 报告 Node 22.17.1 / ABI 127 / SQLite 兼容。
- 实际启动验证：脚本未经过 `npm start` 即成功监听 5679；`/health` 与 `/agent-workbench` 均返回 HTTP 200；随后已正常关闭测试服务。

## 2026-09-16 真实生产闭环
- 用户已明确授权接入其本地配置的 AI 供应商，目标升级为可真实自动生产的工作台。
- 开始只读审计供应商配置与现有服务入口；首次从仓库根目录读取相对 SQLite 路径打开了错误数据库，已改为固定 `backend-node/` 工作目录。
- 已确认真实默认能力：文本、图片、分镜图、视频可用，配音尚未配置；未读取或展示密钥正文。
- 已确认可复用的真实服务覆盖故事到合成全链路，开始实现持久化 Agent 编排器与供应商预检接口。
- 已新增真实 Agent 编排器：剧本 → 资产 → 关键帧/视频/可选配音 → 质检 → 合成，支持持久化步骤、任务轮询、并发限制、重试和重启恢复。
- 已完成《午夜便利店》最小真实 Demo 的文本链路：1 集 30 秒剧本、3 个角色、5 个场景、6 个道具和 6 镜分镜均真实生成并落库。
- 修复 DeepSeek thinking 导致结构化结果为空的问题，并新增快速回归测试。
- 修复场景单图缺少 `polished_prompt_single` 列的问题，迁移已在真实数据库执行。
- 验证图片回退：OpenAI 生图失败后自动切换 Seedream；随后供应商明确返回 Seedream `ModelNotOpen`，确认账号尚未开通模型。
- 新增供应商故障熔断、分镜图跨配置回退、中文错误归因、资产为 0 的前后端双重审核止损，以及新建页真实模式自动禁用。
- 后端完整测试 84/84、前端测试 10/10、Vite 生产构建和 `git diff --check` 均通过；后续增量定向测试 14/14 通过。
- 浏览器只读验收通过：真实入口因已知模型故障禁用，资产审核的“通过并继续”禁用并展示修复提示；未触发视频调用。
- 当前外部阻塞：需要在火山方舟开通 `doubao-seedream-4-5-251128` 或配置另一套真实可用图片模型，之后可从现有资产审核节点重跑并继续视频/合成验证。

## 2026-09-16 产品验收与体验修复
- Seedream 开通后真实生成 4 个角色、3 个场景共 7/7 张参考图，0 警告。
- 发现 AI 返回 7 镜而确认计划为 6 镜；新增分镜数量校准，将同场景相邻叙事节拍合并并保持 30 秒总时长，定向测试 14/14 通过。
- 使用 agent-browser 复现侧栏导航无效、审核摘要无效、新建页无法关闭、文字对比度不足和缺少连续 AI 对话五类问题。
- 修复侧栏真实滚动/高亮、审核摘要进入制作页、返回按钮与 Escape、AI 导演修改对话，以及亮/暗主题可读性。
- axe-core 确定违规从 3 类降至 0；桌面与 390×844 手机视口回归通过；验收证据见 `dogfood-output/report.md`。
- 前端生产构建、前端测试 10/10、后端 Agent 定向测试 14/14 均通过。
- 真实 Demo 保持暂停，避免 UI 验收期间意外消耗视频额度。

## 2026-09-16 真实 Demo 导出完成
- 通过资产审核后，Seedream 4.5 完成 6/6 张 9:16 分镜关键帧，MiniMax `speech-2.8-hd` 完成 6/6 段配音。
- Seedance `doubao-seedance-1-5-pro-251215` 的 3 个任务均返回账号无权限；确认属于供应商权限，不是工作台卡死。
- 新增本地 FFmpeg 运镜兜底：为全部 6 镜生成 5 秒、720×1280、25fps H.264 片段，明确标记为本地兜底而非 AI 动态视频。
- 修复失败视频仍计实际成本的问题；本次运行实际成功调用成本从 ¥19.08 校正为 ¥2.58。
- 修复导出时把关键帧 `local_path` 当作视频路径的问题，并让合成优先复用已经生成的旁白音频，避免重复 TTS。
- 终审通过后成功导出《午夜便利店》30.000 秒成片：H.264 720×1280 + AAC 44.1kHz 单声道，约 3.2MB，状态 `EXPORTED`。
- 内置 macOS arm64 FFmpeg/FFprobe；`start_app.command` 已重启成功，`/health` 返回正常。
- 最终回归：后端 85/85、前端 10/10、Vite 生产构建、成片 ffprobe 与六镜抽帧验收均通过。

## 2026-09-16 Seedance 真实视频升级
- 用户确认开通 Seedance 后，用单镜头真实请求建立反馈闭环：权限错误从 404 转为首帧模式 `ratio` 参数冲突 400。
- 新增回归测试并先验证失败；修复火山首帧/首尾帧请求不应携带 `ratio` / `aspect_ratio` 后测试转绿。
- 当前账号实际启用模型为 `doubao-seedance-2-5-260628`；镜头 1、3、5 三个任务均进入上游队列并完成，本地 MP4 为 1440×2560、24fps、带原生音轨。
- 修复视频完成后覆盖 `storyboards.local_path` 关键帧路径的问题，并恢复三个镜头的图片路径；视频路径只保存在 `video_generations`。
- 修复审核记录中的请求模型与实际配置模型不一致，以及相同时间戳时未优先使用本地视频文件的问题。
- 首次混合合成因 24fps Seedance 与 25fps 本地兜底直接流复制，文件漂移到 31.542 秒；改为统一重编码 25fps 并按计划时长截断。
- 最终 Seedance 版成片精确 30.000 秒、1440×2560、25fps、H.264 + AAC，重点动态镜头 3 个、本地运镜 3 个；实际成功调用估算成本更新为 ¥19.08。
- 后端完整测试更新为 86/86 通过。

## 2026-09-17 剧情优先与智能模型路由
- 用户确认各视频模型均已打通，授权工作台按镜头自行选型。
- 用户将内容偏好明确为小说爽文、悬疑、系统类漫剧；质量优先级为剧情完整 > 核心动作与人物稳定 > 局部动画精度。
- 本阶段只修改编排逻辑和测试，不自动触发任何付费生成。
- 已先补三项红色回归：成本模式仍只规划 3/6 个动态视频、尚无模型路由函数、30 秒未进入剧本提示词；测试按预期失败，开始最小实现。
- 已确认用户新增的五个 Seedance 型号均保存在同一条启用配置中，默认型号仍为 2.5，整个检查过程未读取密钥正文。
- 已将剧本生成改为按单集秒数控制篇幅和 token 下限，并加入前三秒钩子、主角目标与主动选择、阻力升级、信息/能力兑现、最后三秒尾钩和动作对白同步约束。
- 已取消“省钱模式只生成约 35% 动态镜头”的设计；新计划与历史计划进入媒体阶段时都为每个有关键帧的分镜提交动态视频，静帧运镜仅保留为上游失败兜底。
- 已实现五档镜头路由并将选中的真实型号写入视频任务与费用记录：Mini 空镜、Fast 单人动作、1.5 Pro 单人对白、2.0 多人连戏、2.5 复杂互动/高潮。
- 已更新 AI 配置页预设与火山一键配置型号列表；定向回归 17/17 通过，五类样例路由与预期完全一致。
- 全量回归通过：后端 88/88、前端 10/10、Vite 生产构建成功、`git diff --check` 通过。
- 已在无运行中生产任务的情况下重启 5679 服务；首次沙箱启动因端口权限失败，获批后成功启动。线上健康检查正常，供应商预检显示文本、图片、分镜图、视频和 TTS 全部 operational。
- 通过只生成计划、不落生产任务的 HTTP 验证确认：1 集 30 秒计划为 6 镜/6 个动态视频，成本策略为全镜头动态化 + 智能模型路由；全过程没有触发付费媒体调用。

## 2026-09-17 道具图配置永久修复
- 已通过数据库任务记录复现项目《死亡弹幕》的“天台铁门/手机”各 3 次失败，实际路由为 OpenAI `gpt-image-2`，错误中的 `gpt-5.4` 来自本地 Codex OAuth 代理内部转换。
- 根因确认：道具图是唯一仍用 YAML `default_image_provider` 覆盖数据库默认图片配置的资产链路；不是配置缓存，重启无法解决。
- 已增加无网络回归测试并先确认修复前稳定失败：未指定模型时，道具图请求错误携带 `preferred_provider=openai`。
- 已移除道具图链路对 YAML `default_image_provider` 的隐式覆盖，未指定模型时统一使用 AI 配置页保存的数据库默认图片配置；用户显式选择型号时仍精确路由。
- 定向回归与后端全量回归通过：89/89；`git diff --check` 通过。测试未访问外部模型、未产生费用。
- 已用 Node 22 一键脚本重启 5679 服务；`/health` 正常，供应商预检确认默认图片与分镜图均为火山引擎 Seedream 4.5，全部能力 operational。

## 2026-09-17 悬疑恐怖自动美术路由
- 用户确认希望直接修改工作台，而非手工研究提示词。
- 已通过数据库与最终场景提示词定位：当前《死亡弹幕》虽为“悬疑反转”，最高优先级画风仍是过于笼统的“2.5D国漫”。
- 本阶段按最小改动实现题材自动推荐、完整画风持久化和场景叙事异常点约束；不会自动调用 Seedream 重绘。
- 已先增加后端与前端红色回归，稳定捕获自动路由、双语提示词持久化、预设展开及叙事异常点五类缺口。
- 已新增“都市怪谈悬疑漫剧”中英文复合预设；工作台在自动模式下会根据悬疑/惊悚/恐怖/怪谈题材推荐，手动画风不被覆盖。
- 已让新项目把完整中英文画风写入 metadata，并让真实角色、场景、分镜链路读取项目画风而非重复传递短标签。
- 已强化场景提取、单图和四视图提示词：悬疑场景必须包含同一克制、可见、服务剧情的叙事异常点，禁止用蜘蛛网/血迹/雾气堆砌替代剧情证据。
- 定向回归通过：后端 33/33、前端画风测试 10/10；未调用外部模型。
- 已将现有《死亡弹幕》的项目画风与对应 Agent 计划迁移到新预设，保留所有旧图片；场景提示词缓存新增画风匹配检查，下次重绘时会自动重建旧的“2.5D国漫”提示词。
- 全量回归通过：后端 96/96、前端 12/12，Vite 生产构建成功。
- agent-browser 验收确认悬疑示例会联动题材和画风，计划弹窗展示“都市怪谈悬疑漫剧（AI 推荐）”，浏览器无运行时错误；同时发现示例文案与数值控件不同步，正在做同域最小修复。
- 已修复示例按钮参数契约：悬疑示例现在同步设置 3 集、60 秒、预算 500；最终计划为 3 集、30 镜、30 个动态镜头、估算 ¥179.30。
- 最终浏览器验收再次确认画风和参数一致，控制台无错误；截图保存在 `/private/tmp/ai-drama-style-smoke/suspense-plan-final.png`。
- 服务已重启并通过 `/health`；`git diff --check` 通过，无 `[DEBUG-...]` 临时日志。整个改造与验收未创建新项目、未调用付费生成。

## 2026-09-17 角色画风同步与提示词缓存修复
- 用户发现《死亡弹幕》的项目画风已切换为“都市怪谈悬疑漫剧”，但角色卡外观描述仍包含“2.5D国漫风格”。
- 已建立四项假设：历史外观文本未清理、角色提示词缓存未失效、按钮作用范围不清、前端缓存；先以真实数据库和接口建立红色回归。
- 本阶段不会自动重新生成角色图，避免产生 Seedream 费用。
- 红色回归确认《死亡弹幕》项目已经是新画风，但 3/3 角色的 appearance 与 polished_prompt 仍携带 2.5D 国漫；不是浏览器缓存。
- 新增“写实电影·都市悬疑”复合预设：真人比例、真实皮肤/毛发/布料、低调布光和都市怪谈电影构图，并明确排除动漫线稿、二次元五官、赛璐璐和塑料 CGI 肤质。
- 角色提取提示词改为“身份事实与画风分离”，即使模型违规返回，入库前也会清理已知旧画风尾句。
- 项目画风变化时会清理角色旧风格描述、让角色/场景/分镜润色缓存失效，并原位替换分镜和道具基础提示词中的可识别旧画风；图片文件不删除。
- “生成文本框架”更名为“补齐文本框架”，悬浮说明明确已有角色不会重写，消除功能预期歧义。
- 已将当前《死亡弹幕》及对应 Agent 计划切换为“写实电影·都市悬疑”，清除角色、场景、分镜和道具文本中的旧画风引用，保留全部已有图片。
- 已同步修正《死亡弹幕》故事创意中遗留的“2.5D国漫画风”字样，避免页面文案继续造成误解。
- 最终回归通过：后端 101/101、前端 13/13、Vite 生产构建、`git diff --check` 均成功；服务已在 5679 重启并通过健康检查。
- agent-browser 只读验收确认页面显示“写实电影·都市悬疑”和“补齐文本框架”，角色正文不再出现“符合/贴合2.5D国漫”，控制台无运行错误；证据为 `dogfood-output/phase10-style-sync.png`。

## 2026-09-17 人物服装连戏校对
- 用户指出苏晴在连续电梯镜头中依次出现“米白参考造型 → 米白外套＋红裙 → 白色造型 → 全红裙”的突兀变化。
- 已建立五项可证伪假设并用当前数据库建立红色检查；检查按预期失败。
- 真实数据确认：角色卡是米白开衫＋浅色长裙，第 3 镜明确一袭红裙，第 4 镜未写服装，第 5/6 镜再次写红裙，且整个区间没有换装动作。
- 第 3—6 镜 `continuity_snapshot` 全为空，确认根因是角色参考图、分镜文本与未实现的连续状态三者冲突，不是单纯 Seedream 质量问题。
- 已新增零模型调用的逐集人物连戏校对器：从角色卡和分镜文本提取服装，建立逐镜状态快照，只有明确换装动作才允许服装变化。
- 图片、专业首/关键/尾帧和视频实际请求均会注入“人物服装连戏最高优先级”；角色参考图被限定为只锁脸型、五官、发型、年龄和体型，冲突服装必须忽略。
- 制作页新增“人物连戏校对”按钮；结果弹窗会列出参考图冲突、无换装突变、自动继承和需要局部重生的镜头，校对本身不调用模型、不产生费用。
- 一键流程在分镜脚本文本完成后自动执行校对；步骤 4 的状态现在明确为“跳过文本重建，仍执行人物连戏校对”。
- 若旧图或旧视频与服装锁冲突，流程只能选择“局部重生并继续”或“停止流程，手动处理”；取消会立即阻断后续图片/视频步骤，不再允许错误素材静默通过。
- 选择局部重生后，步骤 8/9 会显示具体重生镜号，并只把受影响镜头加入图片/视频生成队列；已有合格素材继续复用。
- 已将《死亡弹幕》第 1 集写入连戏快照：林晚第 1—6 镜保持浅灰卫衣＋深色长裤；苏晴第 3—6 镜固定同一件一袭红裙，并明确排除米白开衫＋浅色长裙参考服装。
- 当前旧图仍保留，校对准确提示只需局部重生第 3、4、5、6 镜；本轮未触发任何付费生图或视频生成。
- 回归通过：后端 105/105、前端 13/13、Vite 生产构建、`git diff --check`；服务已重启并通过 `/health`。
- agent-browser 验收通过：按钮可见可点击，弹窗准确显示“6 镜、1 处参考图冲突、1 处自动继承、第 3—6 镜需局部重生”，控制台无错误；证据见 `dogfood-output/phase11/report.md`。
- 质量闸门浏览器验收通过：选择“停止流程，手动处理”后流程立即结束、一键按钮恢复；网络记录只有素材读取 GET，没有图片/视频生成 POST，未产生模型费用。截图见 `dogfood-output/continuity-quality-gate.png`。

## 2026-09-17 手机信息双层构图修复
- 用《死亡弹幕》第 2 镜真实提示词建立红色检查，复现“正面人物表情＋朝向人物的手机屏幕内容”物理矛盾，以及缺少手机背壳防错约束。
- 新增零模型调用的屏幕构图识别与审计：仅命中同时包含屏幕剧情信息和人物反应的镜头，普通拿手机镜头不添加浮层。
- 图片、通用优化和视频提示词统一注入“人物主画面＋侧边手机 UI 浮层”，并禁止背壳发光、透明手机、双面屏、镜像文字、额外手机与额外手指。
- 视频提示词额外锁定浮层在画面坐标中稳定存在，不随实体手机移动、翻转或透视漂移。
- 一键流程已把屏幕构图校对与人物连戏校对合并为零费用质量闸门；旧生成记录缺少新合同就只重生对应镜头。
- 当前第 2、4 镜提示词已更新，旧图片与旧视频均被准确标记为需局部重生；本轮未触发 Seedream、Seedance 或 TTS。
- 回归通过：后端 110/110、前端 13/13、Vite 生产构建成功；浏览器确认三类提示词均显示新合同且无运行时错误，证据见 `dogfood-output/screen-overlay-prompt.png`。

## 2026-09-17 连续开发闭环基线
- 已完成框架与设计目标扫描，详细结果见 docs/project-framework-and-goals.md；今后模块进度统一记录 docs/progress.md。
- 本轮后端 110/110、前端 13/13、139 个 JS 语法检查、Vite 构建、隔离启动与 HTTP Mock 三审核闭环均通过。端口 EPERM 经本机监听权限重试解决，包体积告警保留。
- 当前 Mock 的 EXPORTED 仅为状态完成，尚无本地媒体文件导出验收；下一候选任务为零费用可播放产物闭环。
- 最新禁止付费 API 的约束覆盖历史授权，未修改业务源码或生产数据库。
