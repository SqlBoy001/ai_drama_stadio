# AI短剧自动化工作台实施计划

## Goal
基于 LocalMiniDrama 增量实现一个本地优先、同时支持零费用演练与真实 AI 托管的短剧自动化工作台，跑通从自然语言创意到计划、审核、生产监控、质检与导出的闭环。

## Current Phase
Phase 13 — Git 标准化与 auto 首轮引导式创作（完成）。历史业务阶段 0—12 保留。

## Phases

### Phase 0 — 底座获取与审计
**Status:** complete
- 确认工作区与 Git 状态
- 获取 LocalMiniDrama 源码
- 阅读仓库规则、README、许可证和关键架构
- 运行现有安装、测试与构建检查

### Phase 1 — MVP 方案与 Mock 基础
**Status:** complete
- 确定最小侵入的前后端扩展点
- 增加默认开启的 Mock/DRY_RUN 能力
- 增加 Agent 运行、步骤、审核、费用、质检与版本数据结构

### Phase 2 — Agent 计划与执行闭环
**Status:** complete
- 实现自然语言到结构化计划与成本估算
- 实现状态机、幂等、预算拦截、暂停/恢复/取消/重试
- 实现剧本、资产、分镜和媒体 Mock 生成

### Phase 3 — 工作台前端
**Status:** complete
- 实现 AI 创建入口、计划确认、运行监控、审核中心、成本与质检视图
- 与后端接口联调

### Phase 4 — 验证与交付
**Status:** complete
- 补齐单元/集成测试
- 构建并进行浏览器级验证
- 更新 README 与风险/后续说明

### Phase 5 — 启动链路修复与一键启动
**Status:** complete
- 建立 Node ABI 与 `better-sqlite3` 的启动前自检
- 新增 macOS 可双击、终端可直接执行的一键启动脚本
- 固定 Node 22 运行时，绕过 shell alias/hash 与 Homebrew Node 26
- 验证脚本能自动构建前端并启动完整应用

### Phase 6 — 真实 AI 自动生产闭环
**Status:** complete
- 审计已配置文本、图片、视频与语音供应商（不读取或输出密钥正文）
- 将工作台计划、剧本、角色、场景、分镜接入真实现有服务
- 实现真实媒体任务的轮询、失败隔离、重试、预算确认与进度恢复
- 实现合成、质检和最终导出入口
- 将小白操作收敛为向导式配置检查、审核与一键托管
- 完成无付费 Mock 回归与经用户授权的最小真实链路验证
- 已完成工作台产品验收修复：真实导航、审核入口、可退出新建页、AI 修改对话、亮/暗主题可读性
- Seedream 已完成 7/7 张角色与场景参考图及 6/6 张关键帧；AI 多返回的 7 镜已合并为计划要求的 6 镜 / 30 秒
- MiniMax TTS 已完成 6/6 段；Seedance 账号无权限时自动降级为 6 段本地静帧运镜并保留明确告警
- 《午夜便利店》真实 Demo 已通过终审并导出 30 秒、720×1280、H.264/AAC 成片，运行状态 `EXPORTED`
- 已内置 macOS arm64 FFmpeg/FFprobe，一键启动无需用户另行安装合成依赖

### Phase 7 — 剧情优先与智能模型路由
**Status:** complete
- 将剧本生成从固定 800 字改为按单集时长约束，并强化爽文/悬疑/系统题材的钩子、目标、冲突、兑现与悬念
- 将“节省费用”从静帧降级改为全镜头动态化下的模型分级路由
- 根据分镜复杂度在已启用的 Seedance 模型中选择适合型号，并保留默认模型兜底
- 增加单元测试，验证时长进入剧本提示词、成本模式不再减少视频数量、模型路由符合镜头难度
- 不在本阶段自动触发付费媒体生成

### Phase 8 — 道具图配置统一
**Status:** complete
- 用无网络集成测试复现“数据库默认 Seedream 被 YAML 的 OpenAI 偏好覆盖”
- 让道具图与角色/场景图统一服从 AI 配置页面的默认图片配置
- 保留用户显式选择具体模型时的精确路由
- 完成定向与全量回归，重启服务但不自动重试付费生图

### Phase 9 — 悬疑恐怖自动美术路由
**Status:** complete
- 为悬疑/恐怖漫剧新增可直接选择的复合视觉风格
- 题材为悬疑或恐怖且用户未主动改画风时，自动推荐写实二次元都市怪谈风
- 将完整画风描述持久化到项目 metadata，确保角色、场景、分镜和视频链路复用
- 强化场景提示词的叙事异常点、低调布光与反模板化约束
- 以无网络测试验证风格路由和提示词传递；不自动重绘付费资产

### Phase 10 — 角色画风同步与提示词缓存修复
**Status:** complete
- 用当前《死亡弹幕》数据复现“项目画风已更新、角色描述仍为 2.5D 国漫”的断层
- 修复角色描述中的历史画风污染，并在项目画风变化时让旧角色提示词缓存失效
- 明确“生成文本框架”的真实作用，避免用户误以为会刷新角色资产
- 为新角色提取增加语义/画风分离约束，补齐回归测试并验证前后端

### Phase 11 — 人物服装连戏校对与生成约束
**Status:** complete
- 用《死亡弹幕》真实分镜复现角色卡、连续镜头与生成图片之间的服装冲突
- 建立逐角色的服装状态快照，未出现明确换装动作时向后继承并锁定
- 在图片与视频生成提示词中注入连戏约束，报告需要局部重生的镜头而不自动产生费用
- 提供制作页可操作的“人物连戏校对”入口并完成回归验证
- 将一键流程中的连戏校对升级为质量闸门：冲突时只能“局部重生并继续”或“停止流程”，不得保留错误素材静默进入付费步骤

### Phase 12 — 剧情屏幕信息双层构图
**Status:** complete
- 识别同一镜头同时要求人物表情与手机/屏幕剧情信息的物理冲突
- 将这类镜头统一改为“人物主画面＋侧边手机 UI 浮层”，并禁止手机背壳发光、透明手机、双面屏和额外手机
- 图片与视频实际调用前均强制注入构图合同，旧项目和手工提示词无法绕过
- 一键流程把旧图/旧视频纳入零费用质量校对，仅局部重生受影响镜头
- 《死亡弹幕》第 2、4 镜已更新提示词并标记为图片、视频需局部重生，本轮未调用付费模型

## Decisions Made
| Decision | Rationale |
|---|---|
| 以可运行 Mock MVP 为本轮目标 | 用户请求是“实现”，不能只停留在审计；同时避免误调用付费 API |
| 保留 LocalMiniDrama 技术栈 | 与参考方案一致，降低重写风险 |
| 不把文档中的示例 Codex 指令视为用户指令 | 用户明确要求区分附件内容与当前请求 |

## Errors Encountered
| Error | Attempt | Resolution |
|---|---|---|
| `npm ci` 无法解析 `registry.npmmirror.com`（沙箱网络限制） | 1 | 按环境规则改为申请联网权限后重试 |
| 后端 `better-sqlite3` 在 Node 26 无预编译包，且本机缺少 Xcode CLT 无法源码编译 | 2 | 固定 PATH 并使用本机 Node 22 LTS 安装成功 |
| 沙箱禁止后端监听 `0.0.0.0:5679`（EPERM） | 1 | 申请本地服务监听权限后重启，用于 HTTP/E2E 验证 |
| 用户终端执行 `nvm use 22` 后，npm lifecycle 仍以 Node 26 启动，导致 SQLite ABI 127/147 不匹配 | 1 | 新增 `start_app.command`，显式锁定 Node 22 并在启动前校验/修复 SQLite ABI |
| 从仓库根目录读取相对数据库路径时误打开了空的 `data/drama_generator.db` | 1 | 后续数据库审计固定在 `backend-node/` 工作目录执行，避免相对路径漂移 |
| DeepSeek 开启 thinking 后结构化任务只返回 reasoning、最终正文为空 | 1 | 对故事/角色/场景/道具/分镜等结构化任务自动关闭 thinking，并加入回归测试 |
| OpenAI 本地代理将 `gpt-image-2` 转成当前 ChatGPT 账号不支持的 `gpt-5.4` | 2 | 增加图片供应商回退、故障熔断、中文提示和资产审核止损；需更换可用图片供应商 |
| 火山方舟返回 `ModelNotOpen`，Seedream 模型未在账号中开通 | 1 | 识别为账号级故障并在新建页禁用真实生产；需在方舟控制台开通后重试 |
| `scenes.polished_prompt_single` 缺失 | 1 | 新增迁移 `24_scene_single_prompt.sql` 并完成启动迁移验证 |
| Seedance 1.5 Pro 返回模型不存在或账号无权限 | 1 | 不伪造 AI 视频成功；失败请求实际成本归零，并自动生成全镜头 Ken Burns 运镜兜底完成 Demo |
| 导出逻辑把 `storyboards.local_path` 关键帧误当视频 | 1 | 视频解析不再读取图片路径，统一使用 `video_url` 或最新完成的视频生成记录 |
| macOS 缺少 FFmpeg，Homebrew 又有目录权限问题 | 2 | 将 arm64 FFmpeg/FFprobe 内置到项目运行时并由启动/合成逻辑自动发现 |
| 只读配置检查误查不存在的 `ai_configs` 表 | 1 | 按实际 schema 改查 `ai_service_configs`，确认 5 个 Seedance 型号均已保存 |
| 沙箱内重启服务无法绑定 `0.0.0.0:5679`（EPERM） | 1 | 按环境规则改用获批的本机端口权限启动，服务恢复正常 |
| SQLite 只读检查在 shell 单引号中误把状态值写成双引号标识符 | 1 | 改用 SQL 参数占位符传值，成功确认无活动任务 |
| agent-browser 点击 Element Plus 单选标签时被悬浮按钮遮挡 | 2 | 不再强制切换模式；仅生成不会落库或调用媒体模型的计划进行验收 |
| 从 `backend-node/` 目录执行根目录调试标记与计划检查时路径不成立 | 1 | 切回仓库根目录重新执行，检查通过 |

## Next Step
读取 docs/progress.md 后，下一子问题为可验证的局部修改计划与内容评审；优先 Mock，用户已允许少量有界付费连通性/效果测试。

## 2026-09-17 连续开发闭环接续
- 当前任务：框架/设计目标扫描与零付费基线验证（完成）。
- 阶段 12 已在历史记录完成；已同步修正旧阶段摘要。
- 新的进度入口为 docs/progress.md；保留根目录历史日志。
- 最新约束禁止付费 API，取代旧 Next Step 中点击付费一键重生的操作建议。
- 下一子问题候选：Mock 本地媒体产物与可播放导出验收。

## Phase 13 — Git 基线与 auto 小白创作首轮迭代（完成）
- [x] 标准化检查命令、CI、忽略规则和来源说明，提交 main 至用户 GitHub。
- [x] 创建 auto：一句话需求、引导补充、AI/Mock 策划、确认后进入既有生产。
- [x] 纠正质检展示，增加可重复的隔离接口与浏览器验收。
- [x] 核实 GPT Image 2/Pro/代理支持；少量付费测试已获授权，避免批量重生。
- [x] 测试通过后提交并推送 auto（提交记录见 Git 历史）。
