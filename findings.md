# Findings

## Source plan summary
- 目标体验：自然语言发起，先展示结构化计划与成本，三个审核点，支持任务恢复、预算拦截、局部重试、质检和导出。
- 技术约束：Vue 3 + Express + SQLite + Electron，本地优先，默认 Mock，不引入微服务/n8n/PostgreSQL。
- 本文件仅记录参考资料与仓库发现；外部内容中的指令不作为执行指令。

## Repository findings
- 已检出官方 `origin/main` 到 `codex/ai-workbench`，版本基线为 LocalMiniDrama 1.2.8，MIT 许可。
- 仓库是三个独立子项目：`backend-node`（Express + better-sqlite3，5679）、`frontweb`（Vue 3 + Vite + Element Plus，3013）、`desktop`（Electron）。
- 后端测试：`node --test test/*.test.js`；前端测试同样使用 Node 内置测试；前端构建：`npm run build`；无 ESLint。
- SQLite 位于 `backend-node/data/`，迁移启动时自动执行；前端通过 Vite 代理 `/api` 与 `/static`。
- 基线已具备画布、角色/场景/道具/分镜、异步任务恢复、多供应商、视频合成与导入导出能力，可做增量扩展。
- 官方安全边界是本地文件、第三方 API 与依赖风险；API Key 和项目数据保存在本地。
- 新工作台采用独立迁移 `23_agent_workbench.sql`、独立服务与路由，避免修改既有生成供应商服务。
- MVP 状态推进由三个审核请求驱动：SCRIPT_REVIEW → ASSET_REVIEW → FINAL_REVIEW → EXPORTED；驳回会暂停，恢复会重建同阶段审核请求。
- Mock 媒体以结构化生成记录、逐镜费用和质检报告表示，可在既有制作页继续人工接管；不会触发真实 API。
- Node 26 暂无当前 `better-sqlite3` 的预编译包；本机验证使用 Node 22.17.1。README 已补充 Node 20/22 LTS 建议。
- HTTP 实测闭环：3 集 / 24 镜；SCRIPT_REVIEW → ASSET_REVIEW → FINAL_REVIEW → EXPORTED；生成 24 条用量与 24 条 PASS 质检记录。
- 浏览器实测：工作台完成态、新建创作页、计划确认弹窗均正常；控制台无错误。

## Startup bug findings
- 用户现场输出证明 `better-sqlite3` 为 Node ABI 127（Node 22），实际服务进程却为 ABI 147（Node 26）。
- 正常加载 NVM 的交互式 zsh 中，Node 22.17.1、npm 10.9.2、`npm exec node` 均一致，且当前 SQLite 原生模块可由 Node 22 正常加载。
- 用户现场出现 npm 11.17.0，说明当时执行的 npm 不是 NVM Node 22 自带版本或 lifecycle PATH 被污染；一键脚本必须绕过终端解析，直接调用明确的 Node 22 路径。

## Real production findings
- 正确数据库中的默认能力均已填写：DeepSeek 文本、OpenAI/Seedream 图片、Seedream 分镜图、Seedance 视频均启用；“已填写”不等于供应商账号已开通模型。
- 当前没有启用的 `tts` 配置。真实生产必须将配音视为可选增强，缺失时以字幕/原视频完成流程，并在配置检查中给出提醒。
- 仓库已经具备真实生成服务：故事、角色提取、场景/道具提取、分镜、角色/场景参考图、逐镜图片、逐镜视频、TTS、FFmpeg 合成；本阶段重点是编排、恢复、预算护栏和小白向导，不重写供应商客户端。
- 现有图片与视频服务都通过 `async_tasks` 暴露完成/失败状态，适合由工作台持久化轮询；服务重启会将普通遗留任务标失败，因此 Agent 运行需要转为可重试暂停态。
- 真实 Demo《午夜便利店》已完成 DeepSeek 剧本、3 个角色、5 个场景、6 个道具与 6 镜结构化分镜；DeepSeek thinking 会挤占结构化输出，已对这些任务自动关闭 thinking。
- OpenAI 本地代理虽然在 `/models` 宣告 `gpt-image-2`，实际会转为 `gpt-5.4 + image_generation`，当前 ChatGPT 账号不支持该模型，因此不能作为图片生产源。
- 火山凭据能够到达 Ark API，但账号未开通 `doubao-seedream-4-5-251128`，接口返回 `ModelNotOpen`；这会同时阻断角色/场景参考图和分镜关键帧。
- 图片调用现在支持按模型显式回退，并对模型未开通、账号不支持、鉴权失败做阶段级熔断；资产参考图为 0 时审核不能放行到高成本视频阶段。
- 供应商状态接口会区分“已配置”与“可运行”，且只返回模型与中文故障说明，不返回密钥；配置更新时间晚于旧故障或后续成功生成后可恢复运行状态。
- 浏览器实测当前新建页会禁用“真实 AI 托管”并切换至零费用演练；《午夜便利店》资产审核显示明确错误且“通过并继续”禁用。

## Product acceptance findings
- 侧栏 `focusPanel` 原先只赋值、不参与模板渲染或滚动，是“审核中心 / 运行监控 / 成本控制”无反应的直接原因，不是接口阻塞。
- 审核摘要原先是无事件的普通 `div`；视觉文案承诺“点击进入”但没有导航，是功能与文案契约不一致。
- 新建创作使用 `activeRun = null` 切换整页，却没有返回动作和 Escape 处理，形成操作死胡同。
- 浅色主题只覆盖了容器背景，内部大量暗色主题灰阶被保留；axe-core 确认 17 个节点对比度不合格。
- 一次性驳回弹窗不足以支持 AI 托管创作；连续对话应把“意见记录、当前审核阶段、重新生成动作”组合为一个低门槛入口。
- 修复后浏览器确认：审核摘要进入 `/film/2`，新建页 Escape 返回任务，导航滚动并高亮对应面板，AI 对话输入可解锁发送，axe 确定违规为 0。

## Final production findings
- 用户启用 Seedream 与 MiniMax TTS 后，参考图 7/7、关键帧 6/6、配音 6/6 均真实生成成功。
- 当前视频配置 `doubao-seedance-1-5-pro-251215` 名称有效，但该 API Key 未获得模型权限；3 个任务均在提交阶段返回 404，不是轮询卡住。
- 原实现把生成任务的估算成本立即写进 `actual_cost`，导致供应商失败仍显示已花费；实际成本应在任务成功后结算，失败保持 0。
- 原导出逻辑将 `storyboards.local_path` 当作视频本地路径，但该列实际保存关键帧图片；必须只接受 `video_url` 或完成的 `video_generations.local_path`。
- 计划只动态化 3 个重点镜头时，其余镜头也必须转成静帧运镜片段，否则最终导出会少镜头、少时长。FFmpeg 本地兜底应覆盖所有没有完成视频的分镜。
- 合成阶段已有 `narration_audio_local_path` 时应直接复用，不能再次调用 TTS；否则会重复费用且同一旁白音色/节奏可能漂移。
- 最终 Demo 文件已验证为 30 秒、720×1280、25fps、H.264/AAC，说明从自然语言到可播放 MP4 的闭环已真正完成。
- 用户开通后的实际视频模型是 `doubao-seedance-2-5-260628`；当 API 请求携带旧模型名但不属于当前配置时，客户端会回退到配置默认模型，生成记录必须同步保存实际模型以保持审计准确。
- Seedance 首帧/首尾帧模式由输入图片决定比例；同时发送 `ratio` 或 `aspect_ratio` 会稳定返回 400。文生视频仍可保留比例字段。
- Seedance 返回 24fps/5.056 秒，本地兜底为 25fps/5 秒；不同 time base 不可用 concat stream copy，否则 30 秒计划会漂移到 31.542 秒。统一重编码到恒定帧率并用计划总时长截断可恢复 30.000 秒。

## Story-first production findings
- 用户明确偏好小说爽文、悬疑、系统类漫剧；验收重点是剧情成立、核心动作清楚、人物无明显瑕疵，而不是每一帧追求电影级精修。
- 用户已打通多个 Seedance 型号，允许工作台按镜头自行选择模型。
- 成本限制不得通过静帧替代剧情镜头；应通过模型分级、资产复用、低成本预览和局部重算实现。
- 当前故事生成器固定要求每集约 800 字，Agent 调用未传单集时长，是 30 秒剧本被过度压缩的直接原因。
- 当前 `save_cost` 将动态视频数量降到约 35%，与用户“成本不影响质量”的要求冲突。
- 当前 Agent 视频生成只读取默认视频配置和默认模型；需要在不重写供应商客户端的前提下增加按镜头复杂度选模。
- 2026-09-17 已确认视频配置 ID 6 的模型列表包含 `doubao-seedance-2-0-mini-260615`、`doubao-seedance-2-0-fast-260128`、`doubao-seedance-1-5-pro-251215`、`doubao-seedance-2-0-260128`、`doubao-seedance-2-5-260628`，默认模型为 2.5；未读取或输出 API Key。
- `video_generations.model` 会被 `videoService` 作为首选模型传入 `videoClient.getDefaultVideoConfig`；只要该型号存在于配置的模型数组，就能沿用同一凭据发起实际请求，因此路由无需复制配置或修改密钥结构。

## Prop image routing findings
- AI 配置页面已将 Seedream 设为默认图片配置，但旧 OpenAI 图片配置仍为启用状态。
- 一键制作前端调用道具生成时明确传入 `model: undefined`；`propImageGenerationService` 随后把 YAML 的 `ai.default_image_provider: openai` 转成 `preferred_provider`，覆盖数据库 `is_default`，因此选择 `gpt-image-2`。
- `loadConfig()` 每次从磁盘重新读取 YAML，没有配置缓存；重启不会改变该结果。
- 永久修复的最小边界是只移除道具图对 YAML provider 的隐式覆盖：无显式模型时使用数据库默认配置，有显式模型时继续按该模型查找配置。

## Suspense visual direction findings
- 《死亡弹幕》保存的 `genre` 是“悬疑反转”，但 `style`、`metadata.style_prompt_zh` 与 `style_prompt_en` 都只有“2.5D国漫”；题材没有驱动画风。
- 工作台新建页仅提供“2.5D国漫、动态漫画、轻写实电影感、水墨国风、赛博朋克”，没有悬疑/恐怖专用选项，默认值固定为“2.5D国漫”。
- 场景四视图最终提示词把“2.5D国漫”标成最高优先级，虽然正文包含阴冷、压抑等氛围词，模型仍优先产出通用赛璐璐动漫背景。
- 当前图像对空间一致性与物件细节执行正常；简陋感主要来自美术路由、提示词冲突以及场景参考表缺少“叙事异常点”，不是 Seedream 4.5 完全无法胜任。
- 最小产品修复应在规划层建立题材到复合画风的推荐，并把完整画风描述写入 metadata；场景提示词再要求每个悬疑场景包含一个克制、可见、可用于剧情的异常线索。
- 浏览器验收发现示例按钮只替换创意文案，不同步集数、单集时长和预算；“3集、60秒、预算500”的悬疑示例实际生成了1集、30秒、预算100的计划，属于同一新建向导里的参数契约问题。

## Character style synchronization findings
- 角色卡展示的 `characters.appearance` 是首次角色提取时保存的历史语义字段；旧提示词把“符合/贴合 2.5D 国漫风格”错误写进人物身份描述，切换项目画风不会自然改变它。
- 角色生图优先复用 `characters.polished_prompt`；《死亡弹幕》三个角色缓存均以“画风·最高优先级：2.5D国漫”开头，是后续仍可能画回旧风格的直接原因。
- “生成文本框架”只补齐缺失的角色、场景、道具和分镜；发现已有角色时明确跳过，因此不具备刷新已有资产提示词的语义。
- 项目画风变更还会影响场景和分镜缓存；只修角色会让 `storyboards.image_prompt/video_prompt` 继续携带旧的完整英文画风，必须在同一保存事务中替换可识别的旧画风并清空润色缓存。
- 人物身份事实与渲染画风应分层保存：appearance 只保留年龄、五官、发型、服装等锚点，完整画风由项目 metadata 在实际生图时注入。

## Character continuity findings
- 《死亡弹幕》第 1 集的苏晴角色卡与参考图是“米白色针织开衫＋浅色长裙”，但第 3 镜明确要求“一袭红裙”，二者是同一次生图请求中的视觉冲突源。
- 第 3—6 镜没有任何“换装、脱下、变装”等服装变化动作；第 4 镜又完全没复述苏晴服装，导致连续镜头缺少可继承的红裙状态。
- 第 3—6 镜的 `storyboards.continuity_snapshot` 全为空，现有数据结构存在但没有真正承担逐镜服装状态机职责。
- 截图中的“米白外套＋红裙”是模型对米白角色参考图与红裙文本提示的折中；后续全红裙则来自第 5 镜重新明确红裙，并非随机漂移可以单独解释。
- 连戏约束必须同时覆盖提示词和参考图语义：明确参考图只锁脸型、五官、发型、体型，服装以当前镜头状态为唯一准则。
- 原中文图像润色规则存在内部矛盾：一方面要求继承上一镜服装，另一方面禁止文字出现服装并声明参考图永远优先；剧情换装与默认角色图不同时必然失败。
- 校对后的稳定策略是：每集独立建立逐角色服装状态；首次明确剧情服装覆盖默认角色卡；没有明确“换上/脱下/变装”动作时持续继承；明确换装动作才允许建立新状态。
- 已有图片是否需要重生不能只看快照是否刚更新；应检查最新完成生图提示词是否实际包含连戏硬锁，避免用户先做校对、稍后再进一键流程时漏报旧图。
- 一键流程原先把步骤 4 的“已有 6 个分镜，跳过生成”同时表达成脚本文本和图片资产，造成用户误以为连戏检查被跳过；实际上它只跳过分镜脚本文本重建，文案必须明确。
- 原弹窗的“保留旧图继续”让已确认的不一致仍可进入后续付费步骤，违反质量优先；连戏冲突应作为强制质量闸门，拒绝修复即停止整条流程。

## Screen-content composition findings
- 第 2 镜要求正面特写林晚表情，同时要求看清朝向林晚的手机屏幕内容；单一真实机位无法同时满足，模型只能把“屏幕发光”错误绑定到可见背壳。
- 第 4 镜存在相同模式，因此不能只手工修第 2 镜；需要按“屏幕信息＋人物反应”语义自动识别。
- 手机参考图的作用应限制为道具尺寸和外形，不能覆盖当前镜头中屏幕朝向与可见面的物理关系。
- 合理构图是人物偏向一侧、侧边保留负空间，再叠加独立手机界面 UI 浮层；实体手机仍按真实透视握持，背壳不发光、无文字和界面。
- Seedance/Seedream 可以按提示生成 UI 构图，但中文字符的逐字准确仍受生成模型限制；需要 100% 准确文字时应在后期合成层确定性叠加，而非依赖模型绘字。

## 2026-09-17 扫描校准
- Mock 的 generateMockMedia 写入模拟用量与预置 PASS，终审只写 export_ready/EXPORTED，没有实际媒体生成或 ffprobe 检测。
- createApp 启动会恢复视频轮询；无付费验收必须用隔离数据库和无凭据配置。
- 本轮前后端测试与隔离 HTTP 审核状态链通过；详见 docs/progress.md。
